
{\rtf1\ansi\ansicpg1252\deff0\nouicompat\deflang1033{\fonttbl{\f0\fnil\fcharset0 Calibri;}{\f1\fnil Calibri;}}
{\colortbl ;\red0\green0\blue255;}
{\*\generator Riched20 10.0.22621}\viewkind4\uc1 
\pard\sa200\sl276\slmult1\b\f0\fs28\lang9 1.Are the HTML tags and elements the same thing?\b0\par
 Ans.they are not.HTML Tags are building blocks of HTML Page. They tell the browser how it should display content to the user. A tag starts with a < bracket and ends with a > bracket. Most tags exist in pairs in HTML. Tags have an opening and closing part. They are similar, except the closing part has a / sign after the opening bracket.\par
HTML Element includes a start tag, content, and an end tag. HTML Elements are components of the web page. Let's say we created a div block and filled it with some text. Then the text-filled div becomes the component of the HTML Page. The <div></div> tags along with the content inside it becomes a component and HTML Element. This will be rendered and shown to the user.\par
\b 2.What are tags and attributes in HTML? \par
\b0 Ans.HTML tags are what defines where an HTML element starts and where it ends. There is usually an opening bracket followed by the element\rquote s name and, finally, a closing bracket, there is always a start tag and an end tag enclosing an element. Nevertheless, that is not the case for all elements.\\\par
ex.<title></title>\par
<p></p>\par
<b></b>\par
an HTML attribute is what modifies an HTML element. It is usually in the form of unique words that one inserts inside the opening tag. They control the behavior of the element that follows.Every tag has two sections. The first one is usually the name of the attribute, while the other one is its value. The two are usually separated by an equals sign (=). Attributes are inside the start tag of that particular element that needs modification.\par
ex.<element attribute="value">element content</element>\par
\b 3.What are void elements in HTML? \b0\par
Ans.A void element is an element whose content model never allows it to have contents under any circumstances. Void elements can have attributes.\par
ex.area, base, br, col, command, embed, hr, img, input, keygen, link, meta, param, source, track, wbr\par
\b 4.What are HTML Entities? \par
\b0 Ans.entity is a piece of text ("string") that begins with an ampersand (&) and ends with a semicolon (;). Entities are frequently used to display reserved characters, and invisible characters (like non-breaking spaces). You can also use them in place of other characters that are difficult to type with a standard keyboard.\par
ex.&amp;, &lt;, &gt;, &quot;\par
\b 5.What are different types of lists in HTML?\b0\par
Ans.\par
A list refers to any information displayed in a logical or linear form. It is a series of items written together in a meaningful group or sequence and marked by bullet points, numbers, etc. There are three types of lists in HTML:\par
Unordered list or Bulleted list (ul)\par
Ordered list or Numbered list (ol)\par
Description list or Definition list (dl)\par
\b 6.What is the \lquote class\rquote  attribute in HTML?\par
\b0 Ans.The class attribute specifies one or more classnames for an element. The class attribute is mostly used to point to a class in a style sheet. However, it can also be used by a JavaScript to make changes to HTML elements with a specified class.\par
\b 7.What is the difference between the \lquote id\rquote  attribute and the \lquote class\rquote  attribute of HTML elements?\b0\par
Ans.A Class name can be used by multiple HTML elements, while an ID name must only be used by one HTML element within the page\par
ex.id\par
<element id="id_name">\par
\par
In CSS Stylesheet:\par
#id_name \{\par
    // CSS Property\par
\}\par
ex.class\par
<element class="class_name">\par
\par
In CSS Stylesheet:\par
.class \{\par
    // CSS Property\par
\}\par
\b 8.What are the various formatting tags in HTML? \b0\par
Ans.<b> - Bold text;element defines bold text\par
<strong> - Important text;element defines text with strong importance\par
<i> - Italic text;element defines a part of text in an alternate voice or mood\par
<em> - Emphasized text;element defines emphasized text\par
<mark> - Marked text; element defines text that should be marked or highlighted\par
<small> - Smaller text;element defines smaller text\par
<del> - Deleted text; element defines text that has been deleted from a document\par
<ins> - Inserted text;element defines a text that has been inserted into a document\par
<sub> - Subscript text;element defines a text that has been inserted into a document\par
<sup> - Superscript text;lement defines superscript text\par
\b 9.How is Cell Padding different from Cell Spacing? \par
\b0 Ans.Both Cellspacing and Cellpadding are the attributes that one can use in table formatting. They basically set those whitespaces in any given table cell.major difference between Cellpadding and Cellspacing in their functionality,We majorly use the Cellpadding for fixing the width present between a cell edge and the content that it contains. Conversely, we use Cellspacing for setting spaces between individual cells.\par
\b 10.How can we club two or more rows or columns into a single row or column in an HTML table?\b0\par
Ans.We use the colspan and rowspan attribute, to merge cells in HTML. The rowspan attribute is for the number of rows a cell should merge, whereas the colspan attribute is for the number of columns a cell should merge. The attribute should be placed inside the <td> tag\par
ex.<td rowspan="2">cell data</td>\par
<td colspan="2">cell data</td>\par
\b 11.What is the difference between a block-level element and an inline element? \b0\par
Ans.Block elements always start from a new line. Inline elements never start from a new line. Block elements cover space from left to right as far as it can go. Inline elements only cover the space as bounded by the tags in the HTML element.\par
\b 12.How to create a Hyperlink in HTML?\b0\par
Ans.The HTML <a> tag defines a hyperlink.The HTML <a> tag defines a hyperlink.\par
ex.<a href="{{\field{\*\fldinst{HYPERLINK https://CarrerCenterTops.com/ }}{\fldrslt{https://CarrerCenterTops.com/\ul0\cf0}}}}\f0\fs28 ">Visit TopsTechonologies.com!</a>\par
\b 13.What is the use of an iframe tag?\b0\par
Ans.The <iframe> tag specifies an inline frame.An inline frame is used to embed another document within the current HTML document.Use CSS to style the <iframe>\par
\b 14.What is the use of a span tag? Explain with example?\b0\par
Ans.The <span> tag is an inline container used to mark up a part of a text, or a part of a document. The <span> tag is easily styled by CSS or manipulated with JavaScript using the class or id attribute. The <span> tag is much like the <div> element, but <div> is a block-level element and <span> is an inline element.\par
ex.p>My mother has <span style="color:blue">blue</span> eyes.</p>\par
\b 15.How to insert a picture into a background image of a web page?\par
\b0 Ans.In the body tag, specify a background image in the background attribute by passing the URL of the image or location path.Adding CSS styling properties.\tab\par
ex.<body background = "URL or path" > Website Body </body>\par
\b 16.How are active links different from normal links?\par
\b0 Ans.Normal links are links which are there on the page and have not been clicked yet. Active links are those links, which have just been clicked at that instant.\par
If you left or right-click any one of the links Visited or Unvisited, it will turn into Red and Underline. Active Links shows that the browser is in the process to load a new resource.  You can do your own custom styling using CSS :active selector.\par
\b 17.What are the different tags to separate sections of text?\b0\par
Ans.There are three tags that can be used to separate the texts:\par
<br> tag \f1\endash  Usually <br> tag is used to separate the line of text. It breaks the current line and conveys the flow to the next line\par
<p> tag \endash  This contains the text in the form of a new paragraph.\par
<blockquote> tag \endash\f0  It is used to define a large quoted section. If you have a large quotation, then put the entire text within <blockquote>\'85\'85\'85.</blockquote> tag.\f1\par
\b\f0\lang16393 18.What is SVG? \b0\par
Ans.SVG is used to describe the two-dimensional vector and vector or raster graphics. SVG images and their behaviors are defined in XML text files. So as XML files, you can create and edit an SVG image with the text editor. It is mostly used for vector type diagrams like pie charts, 2-Dimensional graphs in an X, Y coordinate system.\par
ex.\par
<svg width="100" height="100">\par
<circle cx="50" cy="50" r="40" stroke="yellow" stroke-width="4" fill="red" />\par
</svg>\par
\b 19.What is difference between HTML and XHTML?\b0\par
Ans.\par
1.HTML stands for Hypertext Markup Language, while XHTML stands for Extensible Hypertext Markup Language.   \par
2.It was developed by Tim Berners-Lee, while It was developed by W3C i.e World Wide Web Consortium.\par
3.It was developed in 1991,  while It was released in 2000.\par
4.It is extended from SGML, while It is extended from XML and HTML.\par
5.The format is a document file format, while The format is a markup language.\par
6.All tags and attributes are not necessarily to be in lower or upper case,while In this, every tag and attribute should be in lower case.\par
7.Doctype is not necessary to write at the topwhile Doctype is very necessary to write at the top of the file.\par
8.It is not necessary to close the tags in the order they are opened, while It is necessary to close the tags in the order they are opened.\par
9.While using the attributes it is not necessary to mention quotes.  For e.g. <Geeks>, While using the attributes it is mandatory to mention quotes.  For e.g. <Geeks=\rdblquote GFG\rdblquote >.\par
10.Filename extension used are .html, .htm,while Filename extension are .xhtml, .xht, .xml.\lang9\par
\par
\par
}
�
